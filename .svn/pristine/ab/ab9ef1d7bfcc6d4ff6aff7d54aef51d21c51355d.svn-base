import api from '../../api/api.js'
Page({
  data: {

  },
  // 缴纳党费点击事件
  btnPayParty: function (event) {
    wx.navigateTo({
      url: '../home/payPartyCost/payPartyCost'
    })
  },
    // 党费记录查询点击事件
      btnPartyRecordQuery: function (event) {
        // 开启震动
        wx.vibrateLong(),
        wx.navigateTo({
          url: '../home/partyRecordQuery/partyRecordQuery'
        });
      },
      // 服务指南点击事件
      btnServiceGuide: function (event) {
        wx.navigateTo({
          url: '../home/serviceGuide/serviceGuide'
        });
        //模拟请求自己服务器
        api.getVolIdList({
          success: (res) => {
            console.log("res", res)
            if (res.data.res === 0) {
              console.log("api.getVolIdList","api.getVolIdList")
            }
          }
        })
      }
})